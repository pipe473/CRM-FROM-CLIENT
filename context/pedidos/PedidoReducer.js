import {
    SELECT_CUSTOMER,
    SELECT_PRODUCT,
    PRODUCTS_QUANTIY
} from '../../types';

export default ( state, action ) => {
    switch(action.type) {


        default: 
            return state
    }
}